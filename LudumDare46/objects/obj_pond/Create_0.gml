// get list of all touching solid objects which denote the pond region and convex shapoe
	// collision_rectangle_list(bbox_left, bbox_top, bbox_right, bbox_bottom, ...);
	
// ds_list --> of pond objects
// pick one of those at random

// pick a point inside of that randomly
// x = random_range(x, x + sprite_width);
// y = random_range(y, y + sprite_height);

// create hidden fish object
